# Excluir clases gRPC del restart class loader de devtools
# Esto evita el error "Lookup method resolution failed"
restart.exclude.grpc-proto=/com\.minigithub\.grpc\.proto\..*/
restart.exclude.grpc-stub=/io\.grpc\..*/
